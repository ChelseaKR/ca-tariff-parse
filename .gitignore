# Source documents are fetched, not committed. See docs/adr/0003.
sources/*.pdf
requirements.audit.txt

__pycache__/
*.py[cod]
.venv/
venv/
dist/
build/
*.egg-info/

.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

.DS_Store

# The scheduled watch keeps its observation log on the watch-log branch; a
# local copy (make watch-log, or a local `watch` run) is not committed here.
data/watch-log.jsonl
data/watch-log.jsonl.tmp
